apiVersion: v1
kind: ConfigMap
metadata:
  name: image-api-config
  namespace: image-api
data:
  # Application configuration
  PORT: "8080"
  LOG_LEVEL: "info"
  MAX_IMAGE_SIZE: "10485760"  # 10MB in bytes
